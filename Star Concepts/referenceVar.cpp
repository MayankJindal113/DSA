// int p;
// int *a= &p;
// Here the &p passes the address of p to ptr a;
// int &a = p; Reference variable ; same memory different variable
// Here a variable is being assingned to the same memory as p
// but address is not shared 
